import { assert } from 'chai';
import { createStore } from 'redux';
import iso, { reducer, loader, clearAll } from '../src/index';

const noop =  () => {};

describe('iso redux.', () => {
  it('should run a no-op', () => {

  });
});
